on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@
      - uses: ./
      - name: test read
        run: |
          ./index.js